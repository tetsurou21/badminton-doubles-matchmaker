# バドミントン ダブルス組み合わせメーカー 仕様書

本書は、`index.html` に実装されている「バドミントン ダブルス組み合わせメーカー」のシステム仕様・画面仕様・データ構造・生成アルゴリズム等をまとめた仕様書です。

---

## 1. システム概要

### 1.1 目的
バドミントンやテニス等のサークル・練習会において、参加人数やコート面数に応じたダブルスの試合組み合わせを、**公平かつ重複を最小限に抑えて**自動生成・管理するためのWebアプリケーション。

### 1.2 主な特徴
- **単一ファイル完結**: HTML/CSS/JavaScriptが単一の `index.html` にまとまっており、Webサーバーや外部通信なしでブラウザ単体（ローカルファイル含む）で動作。
- **公平な参加率制御**: 途中参加や一時休憩のメンバーがいる場合でも、「参加機会に対する出場率」を基に出場者を優先選出。
- **重複最小化アルゴリズム**: 過去に組んだペア回数や対戦回数をペナルティ計算し、未対戦・未ペアの組み合わせを優先。
- **直感的な操作性**:
  - タップによる選手の手動入れ替え（スワップ）。
  - 直前の組み合わせ取り消し（Undo）。
  - ペア回数クロス集計表（マトリックス）のリアルタイム表示。
- **ローカルストレージ永続化**: ブラウザを再読み込み・閉じてもデータが維持される。

---

## 2. システム構成・技術スタック

| 項目 | 内容 |
| :--- | :--- |
| **アーキテクチャ** | クライアントサイドSPA（Single Page Application） |
| **フロントエンド技術** | HTML5, CSS3, JavaScript (Vanilla ES6+) |
| **外部ライブラリ** | なし（ゼロ依存関係） |
| **データ永続化** | Web Storage API (`localStorage`) |
| **ストレージキー** | `badminton_matchmaker_v1` |
| **対応環境** | モダンブラウザ（Chrome, Safari, Firefox, Edge）、PCおよびスマートフォン・タブレット |

---

## 3. データ構造 (Data Model)

アプリケーションの状態は単一のグローバルオブジェクト `state` で管理され、変更時に `localStorage` へシリアライズして保存されます。

### 3.1 State スキーマ

```typescript
interface AppState {
  courtCount: number;         // 設定コート面数 (1, 2, 3)
  currentRound: number;       // 現在のラウンド番号 (第N試合)
  masterMembers: MasterMember[]; // サークル名簿（マスターメンバー）
  members: Member[];          // 本日の練習に参加しているメンバー配列
  rounds: Round[];            // 試合履歴配列
  pairCounts: Record<string, number>; // ペア履歴回数 (キー: "id1_id2" ※ID昇順)
  oppCounts: Record<string, number>;  // 対戦相手履歴回数 (キー: "id1_id2" ※ID昇順)
}

interface MasterMember {
  id: string;                 // 一意の選手ID
  name: string;               // 選手名
}
```

### 3.2 Member モデル

| プロパティ | 型 | 説明 |
| :--- | :--- | :--- |
| `id` | `string` | 一意のID（`masterMembers` と共通） |
| `name` | `string` | メンバー表示名 |
| `status` | `'active' \| 'resting' \| 'left'` | 状態（参加中 / 一時休憩 / 途中退出） |
| `matchCount` | `number` | 累積試合出場回数 |
| `availableRounds` | `number` | 自身が「参加中 (active)」だったラウンドの総回数（参加可能数） |
| `consecutiveRest`| `number` | 連続待機ラウンド数（タイブレーク用） |
| `consecutivePlay`| `number` | 連続出場ラウンド数（タイブレーク用） |

### 3.3 Round モデル

```typescript
interface Round {
  roundNumber: number;        // 試合番号
  matches: Match[];           // 当該ラウンドの各コートの対戦情報
  bench: string[];            // 参加中だが出場枠から外れた待機選手のID一覧
  resting: string[];          // 「一時休憩」ステータスの選手ID一覧
}

interface Match {
  court: number;              // コート番号 (1〜courtCount)
  teamA: [string, string];    // チームAの選手ID 2名
  teamB: [string, string];    // チームBの選手ID 2名
}
```

### 3.4 ペア・対戦キー生成ルール
2人の選手ID（`id1`, `id2`）を文字列比較（`<`）し、常に昇順で結合してキーとします。
- キー形式: `"${min(id1, id2)}_${max(id1, id2)}"`

---

## 4. 機能要件 (Functional Specifications)

### 4.1 コート面数設定
- **選択肢**: 1面 (4人必要)、2面 (8人必要)、3面 (12人必要)。デフォルトは2面。
- **UI表示**:
  - 現在の「参加中」人数と「必要」人数（`courtCount * 4`）をリアルタイム表示。
  - 「参加中」人数が4人未満の場合は「次の組み合わせを生成」ボタンを無効化（disabled）。

### 4.2 サークル名簿＆本日の参加メンバー管理

#### (1) サークル名簿（マスターメンバー）の永続管理
- サークル所属者をあらかじめ登録・保持しておく名簿機能。
- 日常の練習を終えて試合履歴をリセットしても、**名簿データは消去されずに保持**されます。

#### (2) 本日の出席者選択モーダル（別画面）
- カードヘッダーの「📋 出席者を選択」ボタンから展開。
- **出席チップ一覧**: タップで出席（チェックON／緑色）と欠席（チェックOFF／白色）をワンタップで切り替え。
- **一括操作**: 「全選択」「全解除」ボタンで全員一括指定が可能。
- **反映**: 「✅ 選択したメンバーを本日の練習に反映」ボタンにより、チェックしたメンバーを本日の練習セッションへ即時展開。
- **名簿管理への動線**: 名簿にいないメンバーを新しく追加したい場合、「⚙️ 名簿の追加・編集画面を開く」から名簿管理画面へ直接切り替え可能。

#### (3) サークル名簿の管理・登録モーダル（別画面・表形式デザイン）
- カードヘッダーの「⚙️ 名簿の管理・登録」ボタンから独立した専用画面として展開。
- **本日の参加メンバー追加と同一の統一デザイン**:
  - **1名追加入力フォーム**: テキスト入力 ＋ 「名簿に追加」ボタン
  - **複数名の一括登録**: テキストエリアによる改行・カンマ区切りでの一括追加
  - **サンプル名簿投入**: 人数セレクタ（4〜25名）＋「投入」ボタン
- **表形式（テーブル表示）による名簿一覧**:
  - 本日のメンバー一覧表と同じクラス（`.member-table`）を採用し、統一された見やすいデザイン。
  - 列構成: `No.`, `名前`, `本日の参加状況`（本日参加中／未選択バッジ表示）, `操作`（削除ボタン ✕）。
  - 各行から名簿メンバーを個別に削除可能。

#### (4) メンバー追加（単一・一括）と自動名簿同期
- メイン画面の「本日の参加メンバー」入力欄からメンバーを追加した場合でも、自動的にサークル名簿（`masterMembers`）へ同期保存されるため、追加したメンバーが次回以降も名簿に残ります。

#### (5) 状態切り替え（3段階トグル）
メンバー一覧のバッジボタンをタップすることで、状態を巡回変更。
```
参加中 (active) ───> 一時休憩 (resting) ───> 途中退出 (left) ───> 参加中 (active)
```
- **参加中 (`active`)**: 試合選出の対象。ラウンド生成時に `availableRounds` が加算される。
- **一時休憩 (`resting`)**: 試合選出から除外。`availableRounds` は加算されない（参加率が不当に低下しない）。
- **途中退出 (`left`)**: 試合選出から除外。取り消し線で表示。

#### (6) メンバー削除
- 本日のメンバー一覧の「✕」ボタン: 本日の練習からのみ削除（名簿には残る）。
- 名簿管理モーダル内の「✕」ボタン: サークル名簿から完全削除。

#### (7) サンプルデータ投入
- 名簿管理モーダル内から人数（4〜25名）を指定して、日本の主要な名字上位25名のサンプルを一括投入可能。

#### (8) 2段階のリセット機能
1. **本日の練習履歴をリセット（名簿は保持）**:
   - サークル名簿（所属者全員）は残したまま、本日の試合履歴・各人の試合数・ペア回数マトリックスのみを綺麗にリセット。次回の練習開始時に使用します。
2. **全データを初期化**:
   - 名簿を含め、ブラウザ内に保存された全てのデータを完全に消去して初期状態に戻します。

---

### 4.3 組み合わせ自動生成アルゴリズム (Core Algorithm)

本アプリケーションの最重要機能であり、以下の2ステップで構成されます。

```mermaid
flowchart TD
    Start([組み合わせ生成ボタン押下]) --> CheckMembers{参加中メンバー >= 4人?}
    CheckMembers -- No --> Alert[警告表示・中断]
    CheckMembers -- Yes --> CalcCourts[利用可能面数を算出<br/>min: 設定面数, floor人数/4]
    
    subgraph Step1 [ステップ1: 出場選手選出]
        CalcCourts --> SortPlayers[参加中メンバーを優先度順にソート<br/>1. 参加率昇順<br/>2. 連続待機数降順<br/>3. 連続出場数昇順<br/>4. ランダムタイブレーク]
        SortPlayers --> SlicePlayers[上位 (面数×4) 名を出場選手に選出<br/>残りを待機選手に決定]
    end
    
    subgraph Step2 [ステップ2: 最適マッチング探索]
        SlicePlayers --> MonteCarlo[モンテカルロ探索 1,800回試行<br/>※1面時は3回]
        MonteCarlo --> CalcScore[ペナルティスコア計算<br/>・ペア重複ペナルティ<br/>・対戦重複ペナルティ]
        CalcScore --> BestFound{スコア0 または 最小スコア更新?}
        BestFound --> UpdateBest[最適組み合わせを保持]
    end
    
    UpdateBest --> CommitRound[状態更新・履歴追加<br/>試合数/待機数/ペア数/対戦数を加算]
    CommitRound --> Render[UI再描画・LocalStorage保存]
```

#### ステップ1: 出場選手の公平選出 (Fairness Selector)
設定コート面数とアクティブ人数から、実際に稼働させる面数 `maxPossibleCourts = min(courtCount, floor(activeMembers.length / 4))` を算出。
必要な選手数 `N = maxPossibleCourts * 4` 名を、以下の優先順位に従ってソートして選出：

1. **参加率（最優先・昇順）**:
   $$\text{参加率} = \frac{\text{matchCount}}{\text{availableRounds}} \quad (\text{availableRounds} = 0 \text{ のときは } 0)$$
   - 参加率の低い選手を優先。
   - 途中参加や休憩復帰した選手は `availableRounds` が少ないため、以降の試合に優先選出されて参加率が平均化される。
2. **連続休憩数（降順）**:
   - 参加率が同等（差が 0.0001 以内）の場合、長く休んでいる選手（`consecutiveRest` が大きい）を優先。
3. **連続試合数（昇順）**:
   - 直前の試合に出ていない選手（`consecutivePlay` が小さい）を優先。
4. **ランダムタイブレーク**:
   - 条件が完全一致した場合はランダム。

#### ステップ2: ペア・対戦の最適化探索 (Penalty Minimization)
選出された `N` 名の選手をコートに割り振る際、組み合わせの重複を排除する探索アルゴリズムを実行。

- **探索回数**: 1面の場合は3回、2面以上の場合は最大 **1,800回** のシャッフルサンプリング。
- **ペナルティ評価関数**:
  各コートのチームA $(p_1, p_2)$、チームB $(p_3, p_4)$ に対してスコアを計算：
  $$\text{Score} = \sum_{\text{courts}} \left( \text{Penalty}_{\text{pair}}(A) + \text{Penalty}_{\text{pair}}(B) + \text{Penalty}_{\text{opp}}(A, B) \right)$$
  - **ペア重複ペナルティ**: 同じ人とペアを組むことを強く抑制（非線形ペナルティ）
    $$\text{Penalty}_{\text{pair}} = (\text{pairCount} \times 100) + (\text{pairCount}^2 \times 80)$$
  - **対戦相手重複ペナルティ**: 同じ人と対戦することを抑制（線形ペナルティ）
    $$\text{Penalty}_{\text{opp}} = \sum_{i \in A, j \in B} \text{oppCount}(i, j) \times 10$$
- **即時終了**: ペナルティスコアが `0`（全ペア・全対戦が初顔合わせ）になった時点でループを中断し高速化。

---

### 4.4 手動入れ替え（スワップ機能）
生成された対戦カード上の選手チップや、待機・休憩リスト内の選手チップをタップすることで、任意に入れ替えが可能。

1. 任意の選手をタップ $\rightarrow$ 選択状態（黄色ハイライト＆パルスアニメーション）。
2. 別の選手をタップ $\rightarrow$ 2名の配置位置（コート内ペアまたは待機/休憩）をスワップ。
3. スワップ完了後、**全履歴から全指標を完全再計算**（`recalculateAllFromScratch()`）し、ペア回数・対戦回数・試合数のデータ整合性を保つ。

---

### 4.5 取り消し (Undo機能)
- 「↩ 1つ前の試合に戻す」ボタン押下時、最新のラウンドを破棄。
- 破棄後、`recalculateAllFromScratch()` を実行して全ステータス（累積試合数・連続数・ペア/対戦マトリックス）を過去ラウンドの記録からゼロクリア再集計。

```javascript
// 再集計ロジックの概要
function recalculateAllFromScratch() {
  // 1. pairCounts, oppCounts を空にリセット
  // 2. 全メンバーの matchCount, availableRounds, consecutivePlay, consecutiveRest を 0 にリセット
  // 3. state.rounds を第1ラウンドから順に走査し、再カウント
}
```

---

### 4.6 各ペアの参加数マトリックス（クロス集計）
- 全登録メンバー同士がこれまでに何回ペアを組んだかを総当たりテーブルで表示。
- **UI工夫**:
  - 列ヘッダ（上部）および行ヘッダ（左部）の sticky 固定配置により、スクロール時も見出しを見失わない。
  - 回数ごとのカラーコーディング:
    - 0回（未ペア）: 白（グレー文字）
    - 1回: 薄緑 (`#ecfdf5`)
    - 2回: 黄色 (`#fef3c7`)
    - 3回以上: 薄赤 (`#fee2e2`)
  - カードヘッダークリックによる開閉（折りたたみ）対応。

---

### 4.7 過去の試合履歴
- 生成された全試合を新しい順（降順）でアコーディオン状に一覧表示。
- 各ラウンドの「各コートの対戦ペア（Team A vs Team B）」および「待機メンバー」「休憩中メンバー」を明記。

---

## 5. 画面仕様・UI設計

### 5.1 全体レイアウト構成
```
+-------------------------------------------------------------+
| Header: 🏸 ダブルス組み合わせメーカー          [第 N 試合]  |
+-------------------------------------------------------------+
| Main Container (メイン画面)                                 |
|  +-------------------------------------------------------+  |
|  | [Nav] メインナビゲーション (3つの画面へ遷移)          |  |
|  |   [👥 本日の参加メンバー] [📊 各ペアのマトリックス]    |  |
|  |   [📁 名簿の管理・登録]                               |  |
|  +-------------------------------------------------------+  |
|  | [1] 組み合わせ生成 & 現在の組み合わせ (コントロール) |  |
|  |   - コート数切替: [1面] [2面 (Active)] [3面]           |  |
|  |   - 参加人数インジケータ (参加中: X名 / 必要: Y名)     |  |
|  |   - [⚡ 次の組み合わせを生成]  [↩ 1つ前を取消]         |  |
|  |   - 現在の対戦カード表示 (第1コート, 第2コート...)       |  |
|  |   - 待機・休憩リスト (☕ 今回のお休み: ...)             |  |
|  +-------------------------------------------------------+  |
|  | [2] 過去の試合履歴 カード                               |  |
|  |   - 最新ラウンドからの逆順カード一覧                   |  |
|  +-------------------------------------------------------+  |
+-------------------------------------------------------------+
| Modal 1: 👥 本日の参加メンバー (オーバーレイ画面)           |
|   - メンバー一覧テーブル (名前, 状態トグル, 試合数, 参加率) |
|   - メンバー追加フォーム (1名追加 / 複数名一括登録)         |
|   - [📋 名簿から出席者を選択] ボタン                        |
|   - [🔄 練習履歴リセット(名簿保持)] / [全データ初期化]     |
|   - [閉じる]                                                |
+-------------------------------------------------------------+
| Modal 2: 📊 各ペアのマトリックス (オーバーレイ画面)         |
|   - 凡例 (0回, 1回, 2回, 3回以上)                          |
|   - 固定見出し付きクロス集計スクロールテーブル              |
|   - [閉じる]                                                |
+-------------------------------------------------------------+
| Modal 3: 📁 サークル名簿の管理・登録 (表形式デザイン)       |
|   - サンプル名簿投入 (4〜25名セレクタ + [投入])             |
|   - 名簿追加フォーム (1名追加 / 複数名一括登録)             |
|   - 表形式の名簿一覧テーブル                                |
|     [No. | 名前 | 本日の参加状況 (バッジ) | 操作 (✕)]       |
|   - [閉じる]  [📋 このまま本日の出席者を選択する →]         |
+-------------------------------------------------------------+
| Modal 4: 📋 本日の出席者を選択 (出席チェック画面)           |
|   - 出席者選択状況 (選択: X / 名簿全体: Y名)                |
|   - [全選択] [全解除]                                       |
|   - 名簿チップ一覧 (タップで出席✓/未選択切替)               |
|   - [⚙️ 名簿の追加・編集画面を開く →]                        |
|   - [キャンセル]  [✅ 本日の練習に反映する]                 |
+-------------------------------------------------------------+
| Toast: メッセージポップアップ (最下部中央・一時表示)        |
+-------------------------------------------------------------+
```

### 5.2 デザインテーマ・カラーパレット
- **Primary**: `#0f766e`（ディープティール）, `#14b8a6`（エメラルドティール）
- **Accent**: `#f59e0b`（アンバー・選択中ハイライト）
- **Danger**: `#ef4444`（レッド・削除・警告）
- **Background**: `#f8fafc`（スレートグレー系ホワイト）
- **Font**: システムフォントスタック（`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Hiragino Kaku Gothic ProN", Meiryo`）

### 5.3 レスポンシブ対応
- **ブレークポイント (640px以下)**:
  - 設定行が縦並びになり、コート切り替えボタンが横幅均等に拡張。
  - 操作ボタン群（生成・取消）が縦並びにスタック（主ボタンが上、取消ボタンが下）。
  - マトリックス表およびメンバー管理表はみ出し時に横スクロール可能（`overflow-x: auto`）。

---

## 6. 非機能要件・特記事項

1. **ゼロ・インストール & オフライン動作**:
   - 体育館などの電波状況が不安定なオフライン環境でも、事前に保存したHTMLファイルを開くだけで完全動作する。
2. **データの安全性**:
   - ブラウザ更新（F5）やタブの誤終了でも、すべての状態が `localStorage` から自動復元される。
3. **データ整合性の保証**:
   - メンバー削除、手動入れ替え、Undoなどの破壊的操作が行われた場合でも、ラウンド履歴配列を正として再集計を行うため、累積カウントのズレや不整合が発生しない設計。
4. **パフォーマンス**:
   - ペアリング最適化（1,800回ループ）は純粋なJavaScript配列操作と数値計算のみで構成されており、一般的なスマートフォンでも数ミリ秒〜数十ミリ秒以内に完了し、UI描画のブロッキングが発生しない。

---

## 7. 将来の機能拡張案 (Future Roadmap)

- **スコア入力・勝敗管理**:
  - 各コートのスコア（例: 21-18）を入力・保存し、勝率や得失点差を集計する機能。
- **レベル別（ランク別）マッチング**:
  - メンバーに「初級」「中級」「上級」などのレーティングを付与し、実力が拮抗するペアリングやミックスダブルスへの対応。
- **データのエクスポート / インポート**:
  - 参加者名簿や対戦結果をCSVまたはJSON形式でダウンロード・バックアップできる機能。
